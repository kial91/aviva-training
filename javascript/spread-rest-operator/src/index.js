//TODO - Convert the following statement using the spread operator
let arr1 = [0, 1, 2];
let arr2 = [3, 4, 5];
arr1 = arr1.concat(arr2);

//TODO - Write a function that takes in two arrays as arguments, and returns a single array that combines both
function combineArrays(arr1, arr2) {

}
let arrays = [[0, 1, 2], [3, 4, 5]];

//TODO - Write a function that can take in any number of arguments (including numbers or strings), and returns the sum of only the numbers.
function sum() {

}

let total = 0;

//TODO - Convert the following statement using the spread operator
let obj1 = { foo: "bar", x: 42 };
let obj2 = { foo: "baz", y: 13 };

let mergedObj = Object.assign(obj1, obj2);

//TODO - Extract 'name' and 'age' into their own variables and store the remaining properties into an 'other' variable
let person = { 
    name: "John", 
    age: 30,
    jobTitle: "Developer",
    address: {
        houseNumber: 1,
        streetName: "High Road"
    }
}; 